# Copy this file to .env and fill in real values. Never commit .env.

# CJdropshipping API key (optional). Get one from https://cjdropshipping.com
# after creating a free account, under API settings. Without this,
# supplier-checker will look up CJ prices manually via the website instead.
CJ_API_KEY=

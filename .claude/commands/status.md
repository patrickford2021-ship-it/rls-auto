---
description: Summarize my progress, money spent, and money made
---

Give a short status report:

1. Read `progress.md` — report the current stage, how many steps are
   checked off in it, and how many stages total are complete.
2. Delegate to `numbers-analyst` to read `data/ledger.csv` and report:
   total spent, total sales revenue, total refunds, and real net profit
   so far (or state plainly if there's no data yet, e.g. "no transactions
   logged yet — that's expected this early").
3. End with the single next step (same as `/next`), in one or two
   sentences.

Keep the whole thing short and scannable — this is a quick check-in, not a
full report.
